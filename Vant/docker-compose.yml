services:
  android-builder:
    # Verified stable tag for March 2026
    image: ghcr.io/cirruslabs/android-sdk:35
    volumes:
      - .:/project
    working_dir: /project
    # Automatically fixes permissions and triggers the build
    command: bash -c "chmod +x gradlew && ./gradlew assembleDebug"
